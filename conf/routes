# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

module:testrunner

GET     /                                       App.Index

GET     /redis                                  Redis.Ping
GET     /redis/ping.json                        Redis.Ping
POST    /redis/set                         Redis.Set
POST    /redis/set.json                         Redis.Set
GET     /redis/get/:key                         Redis.Get
GET     /redis/get/:key.json                       Redis.Get

GET     /unko                                   Unko.Index
GET     /unko/list.json                         Unko.List
GET     /unko/show/:id                          Unko.Show
GET     /unko/show/:id.json                     Unko.Show
POST    /unko/cancel/:id                        Unko.Cancel

GET     /single/upload                         Single.Upload
POST     /single/handle_upload                        Single.HandleUpload

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")

# Catch all
*       /:controller/:action                    :controller.:action
